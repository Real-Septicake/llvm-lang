extern sin(num) : num;

fn func2(n : num) : num {
    return 2 * (8 + n);
}

fn func(rep : num) : void {
    for(var i : num = 0; i < rep; i = i + 1) {
        print i;
        if(toBool(i)) {
            var v : bool = toBool(i);
            print v;
        }
    }
    return;
}

func(12);
